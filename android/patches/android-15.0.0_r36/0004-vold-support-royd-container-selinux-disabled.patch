diff --git a/system/vold/Utils.cpp b/system/vold/Utils.cpp
--- a/system/vold/Utils.cpp
+++ b/system/vold/Utils.cpp
@@ -31,6 +31,7 @@
 #include <logwrap/logwrap.h>
 #include <private/android_filesystem_config.h>
 #include <private/android_projectid_config.h>
+#include <selinux/selinux.h>
 
 #include <dirent.h>
 #include <fcntl.h>
@@ -459,8 +460,18 @@ status_t PrepareDir(const std::string& path, mode_t mode, uid_t uid, gid_t gid,
     if (selabel_lookup(sehandle, &tmp_secontext, cpath, S_IFDIR) == 0) {
         secontext.reset(tmp_secontext);
         if (setfscreatecon(secontext.get()) != 0) {
-            LOG(ERROR) << "Failed to setfscreatecon for directory " << path;
-            return -EINVAL;
+            const int saved_errno = errno;
+            const char* royd_container = getenv("ROYD_CONTAINER");
+            if (saved_errno == EINVAL && royd_container != nullptr && royd_container[0] == '1' &&
+                royd_container[1] == '\0' && is_selinux_enabled() <= 0) {
+                LOG(WARNING) << "ROYD: setfscreatecon unavailable with kernel SELinux "
+                                "disabled; continuing for directory "
+                             << path;
+            } else {
+                errno = saved_errno;
+                LOG(ERROR) << "Failed to setfscreatecon for directory " << path;
+                return -EINVAL;
+            }
         }
     } else if (errno == ENOENT) {
         LOG(DEBUG) << "No selabel defined for directory " << path;
diff --git a/system/vold/vold_prepare_subdirs.cpp b/system/vold/vold_prepare_subdirs.cpp
--- a/system/vold/vold_prepare_subdirs.cpp
+++ b/system/vold/vold_prepare_subdirs.cpp
@@ -34,12 +34,19 @@
 
 #include <cutils/fs.h>
 #include <selinux/android.h>
+#include <selinux/selinux.h>
 
 #include "Utils.h"
 #include "android/os/IVold.h"
 
 #include <private/android_filesystem_config.h>
 
+static bool is_royd_selinux_disabled() {
+    const char* royd_container = getenv("ROYD_CONTAINER");
+    return royd_container != nullptr && royd_container[0] == '1' && royd_container[1] == '\0' &&
+           is_selinux_enabled() <= 0;
+}
+
 static void usage(const char* progname) {
     std::cerr << "Usage: " << progname << " [ prepare | destroy ] <volume_uuid> <user_id> <flags>"
               << std::endl;
@@ -71,8 +78,16 @@ static bool prepare_dir_for_user(struct selabel_handle* sehandle, mode_t mode, u
             secontext.reset(tmp_secontext);
         }
         if (setfscreatecon(secontext.get()) != 0) {
-            LOG(ERROR) << "Failed to setfscreatecon for directory " << path;
-            return false;
+            const int saved_errno = errno;
+            if (saved_errno == EINVAL && is_royd_selinux_disabled()) {
+                LOG(WARNING) << "ROYD: setfscreatecon unavailable with kernel SELinux "
+                                "disabled; continuing for directory "
+                             << path;
+            } else {
+                errno = saved_errno;
+                LOG(ERROR) << "Failed to setfscreatecon for directory " << path;
+                return false;
+            }
         }
     } else if (errno == ENOENT) {
         LOG(DEBUG) << "No selabel defined for directory " << path;
@@ -87,7 +102,7 @@ static bool prepare_dir_for_user(struct selabel_handle* sehandle, mode_t mode, u
     if (fs_prepare_dir(path.c_str(), mode, uid, gid) != 0) {
         return false;
     }
-    if (secontext) {
+    if (secontext && !is_royd_selinux_disabled()) {
         char* tmp_oldsecontext = nullptr;
         if (lgetfilecon(path.c_str(), &tmp_oldsecontext) < 0) {
             PLOG(ERROR) << "Unable to read secontext for: " << path;
